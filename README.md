# Linear logic in Ur/Web.

This library implements linear logic (MALL) as a two-sided intuitionistic sequent calculus.
The connectives one, tensor, zero, plus, top, with, lollipop, and ofcourse are implemented.
